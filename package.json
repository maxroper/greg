{
  "name": "gregpryor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "worker:dev": "vite build && wrangler dev",
    "check:deploy-env": "node scripts/check-deploy-env.js",
    "deploy": "npm run check:deploy-env && vite build && wrangler deploy"
  },
  "dependencies": {
    "@stripe/react-stripe-js": "^6.3.0",
    "@stripe/stripe-js": "^9.4.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "@vitejs/plugin-react": "^4.3.4",
    "stripe": "^17.7.0",
    "vite": "^6.0.7",
    "wrangler": "^4.20.0"
  }
}
